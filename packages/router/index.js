import routerPlugin from './install'
import { encodeParams, encodeUrl, decodeParams, decodePage, decodeUrl, createRouter } from '../router/core'

export {
  encodeParams,
  encodeUrl,
  decodeParams,
  decodePage,
  decodeUrl,
  createRouter,
  routerPlugin
}
