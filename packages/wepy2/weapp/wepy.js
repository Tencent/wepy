import page from './page';
import app from './app';
import component from './component';
import $global from './global';

export default {
  component: component,
  page: page,
  app: app,
  global: $global
}
